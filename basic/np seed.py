
import numpy as np
import tensorflow as tf
import matplotlib.pyplot as plt

""" Set Random Seeds for same results """

np.random.seed(101)
rand_a = np.random.uniform(0, 100, (2, 2))
print ('\n',rand_a)

np.random.seed(101)
rand_a = np.random.uniform(0, 100, (2, 2))
print ('\n',rand_a)

rand_a = np.random.uniform(0, 100, (2, 2))
print ('\n',rand_a)